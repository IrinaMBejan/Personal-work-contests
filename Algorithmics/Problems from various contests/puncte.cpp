#include <fstream>

using namespace std;
ifstream fin("puncte.in");
ofstream fout("puncte.out");

int dif[20005],max,sol[20005];

int main()
{
    int x;
   /*while(fin>>x)
    {
        dif[x]++;
        if(x>=max)
            max=x;
    }
    sol[1]=0;
    sol[2]=max;
    dif[max]--;
    bkt(??)*/
    return 0;
}
